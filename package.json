{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "Live SLA truth-bonds for the agent/x402 economy -- a GenLayer Intelligent Contract project.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint:contracts": "genvm-lint check contracts/Sentinel.py && genvm-lint check contracts/SentinelFactory.py",
    "test:direct": "gltest tests/direct",
    "deploy:factory": "tsx deploy/001_deploy_sentinel_factory.ts"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
